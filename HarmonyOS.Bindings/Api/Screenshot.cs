// <auto-generated>
// 由 apiGenerator.ts 生成（@ohos.* namespace 绑定路线）。
// 生成器输入：HarmonyOS SDK .d.ts → astParser → ComponentInfo → 本生成器
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;
using HarmonyOS.Bindings.Runtime;
using HarmonyOS.ArkUI;

namespace HarmonyOS.Bindings.Api;

/// <summary>
/// Screenshot 绑定（@ohos.screenshot）。
/// 所需权限：ohos.permission.CUSTOM_SCREEN_CAPTURE
/// </summary>
public static unsafe partial class Screenshot
{
    private const string ModuleName = "@ohos.screenshot";

    private static NapiReference? _moduleRef;
    private static bool _loadAttempted;

    /// <summary>懒加载的 @ohos 模块对象（internal：同文件包装类的构造函数需要）</summary>
    internal static IntPtr Module
    {
        get
        {
            if (_moduleRef != null) return _moduleRef.Value;
            if (_loadAttempted)
                throw new InvalidOperationException($"{ModuleName} module failed to load (previous attempt)");
            _loadAttempted = true;

            var env = NapiEnv.Current;
            NativeNodeApi.napi_open_handle_scope(env, out var scope).ThrowIfFailed();
            try
            {
                foreach (var name in new[] { "=" + ModuleName, ModuleName })
                {
                    var utf8 = System.Text.Encoding.UTF8.GetBytes(name);
                    fixed (byte* p = utf8)
                    {
                        var status = NativeNodeApi.napi_load_module(env, p, out var module);
                        if (status == NativeNodeApi.napi_status.napi_ok && module != IntPtr.Zero)
                        {
                            _moduleRef = new NapiReference(module);
                            break;
                        }
                        NativeNodeApi.napi_get_and_clear_last_exception(env, out _);
                    }
                }
            }
            finally
            {
                NativeNodeApi.napi_close_handle_scope(env, scope).ThrowIfFailed();
            }

            if (_moduleRef == null)
                throw new InvalidOperationException(
                    $"failed to load {ModuleName} via napi_load_module (tried with and without '=' prefix)");

            HiLog.Info("HarmonyHost", $"[screenshot] module loaded via napi_load_module");
            return _moduleRef.Value;
        }
    }

    private static ReadOnlySpan<byte> _capture => "capture"u8;
    private static ReadOnlySpan<byte> _pick => "pick"u8;

    /// <summary>
    /// capture
    /// </summary>
    public static Task<IntPtr> CaptureAsync(CaptureOption? options = null)
    {
        return NodeApi.CallMethodAsync<IntPtr>(Module, _capture, options);
    }

    /// <summary>
    /// pick
    /// </summary>
    public static Task<IntPtr> PickAsync()
    {
        return NodeApi.CallMethodAsync<IntPtr>(Module, _pick);
    }

}

/// <summary>
/// CaptureOption（@ohos 命名空间内嵌套纯数据接口，入参对象）。
/// </summary>
public sealed record CaptureOption(
    double? DisplayId = null,
    double[]? BlackWindowIds = null
) : INapiRecord
{
    private static ReadOnlySpan<byte> _displayIdName => "displayId"u8;
    private static ReadOnlySpan<byte> _blackWindowIdsName => "blackWindowIds"u8;
    void INapiRecord.WriteTo(NativeNodeApi.napi_env env, NativeNodeApi.napi_value obj)
    {
        var _displayIdV = NativeValue.From(DisplayId);
        if (_displayIdV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _displayIdName, _displayIdV);
        var _blackWindowIdsV = NativeValue.From(BlackWindowIds);
        if (_blackWindowIdsV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _blackWindowIdsName, _blackWindowIdsV);
    }
}
