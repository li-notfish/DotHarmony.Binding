// <auto-generated>
// 由 apiGenerator.ts 生成（@ohos.* namespace 绑定路线）。
// 生成器输入：HarmonyOS SDK .d.ts → astParser → ComponentInfo → 本生成器
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;
using HarmonyOS.Bindings.Runtime;
using HarmonyOS.ArkUI;

namespace HarmonyOS.Bindings.Api;

/// <summary>
/// LinkEnhance 绑定（@ohos.distributedsched.linkEnhance）。
/// 所需权限：ohos.permission.DISTRIBUTED_DATASYNC
/// </summary>
public static unsafe partial class LinkEnhance
{
    private const string ModuleName = "@ohos.distributedsched.linkEnhance";

    private static NapiReference? _moduleRef;
    private static bool _loadAttempted;

    /// <summary>懒加载的 @ohos 模块对象（internal：同文件包装类的构造函数需要）</summary>
    internal static IntPtr Module
    {
        get
        {
            if (_moduleRef != null) return _moduleRef.Value;
            if (_loadAttempted)
                throw new InvalidOperationException($"{ModuleName} module failed to load (previous attempt)");
            _loadAttempted = true;

            var env = NapiEnv.Current;
            NativeNodeApi.napi_open_handle_scope(env, out var scope).ThrowIfFailed();
            try
            {
                foreach (var name in new[] { "=" + ModuleName, ModuleName })
                {
                    var utf8 = System.Text.Encoding.UTF8.GetBytes(name);
                    fixed (byte* p = utf8)
                    {
                        var status = NativeNodeApi.napi_load_module(env, p, out var module);
                        if (status == NativeNodeApi.napi_status.napi_ok && module != IntPtr.Zero)
                        {
                            _moduleRef = new NapiReference(module);
                            break;
                        }
                        NativeNodeApi.napi_get_and_clear_last_exception(env, out _);
                    }
                }
            }
            finally
            {
                NativeNodeApi.napi_close_handle_scope(env, scope).ThrowIfFailed();
            }

            if (_moduleRef == null)
                throw new InvalidOperationException(
                    $"failed to load {ModuleName} via napi_load_module (tried with and without '=' prefix)");

            HiLog.Info("HarmonyHost", $"[distributedsched.linkEnhance] module loaded via napi_load_module");
            return _moduleRef.Value;
        }
    }

    private static ReadOnlySpan<byte> _createServer => "createServer"u8;
    private static ReadOnlySpan<byte> _createConnection => "createConnection"u8;

    /// <summary>
    /// createServer
    /// </summary>
    public static Server CreateServer(string name)
    {
        return NodeApi.CallMethod(Module, _createServer, static h => new Server(h), name);
    }

    /// <summary>
    /// createConnection
    /// </summary>
    public static ConnectionObject CreateConnection(string deviceId, string name)
    {
        return NodeApi.CallMethod(Module, _createConnection, static h => new ConnectionObject(h), deviceId, name);
    }

}

/// <summary>
/// Server 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class Server : JsObject
{
    public Server(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _start => "start"u8;
    private static ReadOnlySpan<byte> _stop => "stop"u8;
    private static ReadOnlySpan<byte> _close => "close"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;
    /// <summary>
    /// start
    /// </summary>
    public void Start()
    {
        CallMethodVoid(_start);
    }

    /// <summary>
    /// stop
    /// </summary>
    public void Stop()
    {
        CallMethodVoid(_stop);
    }

    /// <summary>
    /// close
    /// </summary>
    public void Close()
    {
        CallMethodVoid(_close);
    }

    /// <summary>
    /// on
    /// </summary>
    public void On(string type, IntPtr callback)
    {
        CallMethodVoid(_on, type, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public void Off(string type, IntPtr callback)
    {
        CallMethodVoid(_off, type, callback);
    }

    private readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<ConnectionObject> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(new ConnectionObject(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public void Off(string type)
    {
        NodeApi.CallMethodVoid(Handle, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<ConnectionObject> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<double> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(NativeValue.ToDouble(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<double> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// 监听 connectionAccepted 事件（对应 on/off）
    /// </summary>
    public event System.Action<ConnectionObject> ConnectionAccepted
    {
        add
        {
            _eventListeners.Add(("connectionAccepted", value),
                args => value(new ConnectionObject(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "connectionAccepted", js));
        }
        remove
        {
            _eventListeners.Remove(("connectionAccepted", value), js => NodeApi.CallMethodVoid(Handle, _off, "connectionAccepted", js));
        }
    }

    /// <summary>
    /// 监听 serverStopped 事件（对应 on/off）
    /// </summary>
    public event System.Action<double> ServerStopped
    {
        add
        {
            _eventListeners.Add(("serverStopped", value),
                args => value(NativeValue.ToDouble(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "serverStopped", js));
        }
        remove
        {
            _eventListeners.Remove(("serverStopped", value), js => NodeApi.CallMethodVoid(Handle, _off, "serverStopped", js));
        }
    }

}

/// <summary>
/// Connection 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class ConnectionObject : JsObject
{
    public ConnectionObject(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _connect => "connect"u8;
    private static ReadOnlySpan<byte> _disconnect => "disconnect"u8;
    private static ReadOnlySpan<byte> _close => "close"u8;
    private static ReadOnlySpan<byte> _getPeerDeviceId => "getPeerDeviceId"u8;
    private static ReadOnlySpan<byte> _sendData => "sendData"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;
    /// <summary>
    /// connect
    /// </summary>
    public void Connect()
    {
        CallMethodVoid(_connect);
    }

    /// <summary>
    /// disconnect
    /// </summary>
    public void Disconnect()
    {
        CallMethodVoid(_disconnect);
    }

    /// <summary>
    /// close
    /// </summary>
    public void Close()
    {
        CallMethodVoid(_close);
    }

    /// <summary>
    /// getPeerDeviceId
    /// </summary>
    public string GetPeerDeviceId()
    {
        return CallMethod<string>(_getPeerDeviceId);
    }

    /// <summary>
    /// sendData
    /// </summary>
    public void SendData(byte[] data)
    {
        CallMethodVoid(_sendData, data);
    }

    /// <summary>
    /// on
    /// </summary>
    public void On(string type, IntPtr callback)
    {
        CallMethodVoid(_on, type, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public void Off(string type, IntPtr callback)
    {
        CallMethodVoid(_off, type, callback);
    }

    private readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<LinkEnhanceConnectResult> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(new LinkEnhanceConnectResult(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public void Off(string type)
    {
        NodeApi.CallMethodVoid(Handle, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<LinkEnhanceConnectResult> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<double> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(NativeValue.ToDouble(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<double> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<byte[]> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(NativeValue.ToByteArray(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<byte[]> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// 监听 connectResult 事件（对应 on/off）
    /// </summary>
    public event System.Action<LinkEnhanceConnectResult> ConnectResult
    {
        add
        {
            _eventListeners.Add(("connectResult", value),
                args => value(new LinkEnhanceConnectResult(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "connectResult", js));
        }
        remove
        {
            _eventListeners.Remove(("connectResult", value), js => NodeApi.CallMethodVoid(Handle, _off, "connectResult", js));
        }
    }

    /// <summary>
    /// 监听 disconnected 事件（对应 on/off）
    /// </summary>
    public event System.Action<double> Disconnected
    {
        add
        {
            _eventListeners.Add(("disconnected", value),
                args => value(NativeValue.ToDouble(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "disconnected", js));
        }
        remove
        {
            _eventListeners.Remove(("disconnected", value), js => NodeApi.CallMethodVoid(Handle, _off, "disconnected", js));
        }
    }

    /// <summary>
    /// 监听 dataReceived 事件（对应 on/off）
    /// </summary>
    public event System.Action<byte[]> DataReceived
    {
        add
        {
            _eventListeners.Add(("dataReceived", value),
                args => value(NativeValue.ToByteArray(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "dataReceived", js));
        }
        remove
        {
            _eventListeners.Remove(("dataReceived", value), js => NodeApi.CallMethodVoid(Handle, _off, "dataReceived", js));
        }
    }

}

/// <summary>
/// ConnectResult 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class LinkEnhanceConnectResult : JsObject
{
    public LinkEnhanceConnectResult(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _deviceId => "deviceId"u8;
    private static ReadOnlySpan<byte> _success => "success"u8;
    private static ReadOnlySpan<byte> _reason => "reason"u8;
    /// <summary>
    /// deviceId
    /// </summary>
    public string DeviceId => NativeValue.ToString(GetPropertyRaw(_deviceId)) ?? string.Empty;

    /// <summary>
    /// success
    /// </summary>
    public bool Success => NativeValue.ToBool(GetPropertyRaw(_success));

    /// <summary>
    /// reason
    /// </summary>
    public double Reason => NativeValue.ToDouble(GetPropertyRaw(_reason));

}
