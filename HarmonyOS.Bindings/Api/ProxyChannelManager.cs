// <auto-generated>
// 由 apiGenerator.ts 生成（@ohos.* namespace 绑定路线）。
// 生成器输入：HarmonyOS SDK .d.ts → astParser → ComponentInfo → 本生成器
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;
using HarmonyOS.Bindings.Runtime;
using HarmonyOS.ArkUI;

namespace HarmonyOS.Bindings.Api;

/// <summary>
/// ProxyChannelManager 绑定（@ohos.distributedsched.proxyChannelManager）。
/// 所需权限：ohos.permission.ACCESS_BLUETOOTH
/// </summary>
public static unsafe partial class ProxyChannelManager
{
    private const string ModuleName = "@ohos.distributedsched.proxyChannelManager";

    private static NapiReference? _moduleRef;
    private static bool _loadAttempted;

    /// <summary>懒加载的 @ohos 模块对象（internal：同文件包装类的构造函数需要）</summary>
    internal static IntPtr Module
    {
        get
        {
            if (_moduleRef != null) return _moduleRef.Value;
            if (_loadAttempted)
                throw new InvalidOperationException($"{ModuleName} module failed to load (previous attempt)");
            _loadAttempted = true;

            var env = NapiEnv.Current;
            NativeNodeApi.napi_open_handle_scope(env, out var scope).ThrowIfFailed();
            try
            {
                foreach (var name in new[] { "=" + ModuleName, ModuleName })
                {
                    var utf8 = System.Text.Encoding.UTF8.GetBytes(name);
                    fixed (byte* p = utf8)
                    {
                        var status = NativeNodeApi.napi_load_module(env, p, out var module);
                        if (status == NativeNodeApi.napi_status.napi_ok && module != IntPtr.Zero)
                        {
                            _moduleRef = new NapiReference(module);
                            break;
                        }
                        NativeNodeApi.napi_get_and_clear_last_exception(env, out _);
                    }
                }
            }
            finally
            {
                NativeNodeApi.napi_close_handle_scope(env, scope).ThrowIfFailed();
            }

            if (_moduleRef == null)
                throw new InvalidOperationException(
                    $"failed to load {ModuleName} via napi_load_module (tried with and without '=' prefix)");

            HiLog.Info("HarmonyHost", $"[distributedsched.proxyChannelManager] module loaded via napi_load_module");
            return _moduleRef.Value;
        }
    }

    private static ReadOnlySpan<byte> _openProxyChannel => "openProxyChannel"u8;
    private static ReadOnlySpan<byte> _closeProxyChannel => "closeProxyChannel"u8;
    private static ReadOnlySpan<byte> _sendData => "sendData"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;

    /// <summary>
    /// openProxyChannel
    /// </summary>
    public static Task<double> OpenProxyChannelAsync(ChannelInfo channelInfo)
    {
        return NodeApi.CallMethodAsync<double>(Module, _openProxyChannel, channelInfo);
    }

    /// <summary>
    /// closeProxyChannel
    /// </summary>
    public static void CloseProxyChannel(double channelId)
    {
        NodeApi.CallMethodVoid(Module, _closeProxyChannel, channelId);
    }

    /// <summary>
    /// sendData
    /// </summary>
    public static Task SendDataAsync(double channelId, byte[] data)
    {
        return NodeApi.CallMethodAsyncVoid(Module, _sendData, channelId, data);
    }

    /// <summary>
    /// on
    /// </summary>
    public static void On(string type, double channelId, IntPtr callback)
    {
        NodeApi.CallMethodVoid(Module, _on, type, channelId, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public static void Off(string type, double channelId, IntPtr callback)
    {
        NodeApi.CallMethodVoid(Module, _off, type, channelId, callback);
    }

    private static readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public static void On(string type, System.Action<DataInfo> callback, double channelId)
    {
        _eventListeners.Add((type, callback),
            args => callback(new DataInfo(args[0])),
            js => NodeApi.CallMethodVoid(Module, _on, type, js, channelId));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public static void Off(string type)
    {
        NodeApi.CallMethodVoid(Module, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public static void Off(string type, System.Action<DataInfo> callback, double channelId)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Module, _off, type, js, channelId));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public static void On(string type, System.Action<ChannelStateInfo> callback, double channelId)
    {
        _eventListeners.Add((type, callback),
            args => callback(new ChannelStateInfo(args[0])),
            js => NodeApi.CallMethodVoid(Module, _on, type, js, channelId));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public static void Off(string type, System.Action<ChannelStateInfo> callback, double channelId)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Module, _off, type, js, channelId));
    }

    /// <summary>
    /// 监听 receiveData 事件（对应 on/off）
    /// </summary>
    public static event System.Action<DataInfo> ReceiveData
    {
        add
        {
            _eventListeners.Add(("receiveData", value),
                args => value(new DataInfo(args[0])),
                js => NodeApi.CallMethodVoid(Module, _on, "receiveData", js));
        }
        remove
        {
            _eventListeners.Remove(("receiveData", value), js => NodeApi.CallMethodVoid(Module, _off, "receiveData", js));
        }
    }

    /// <summary>
    /// 监听 channelStateChange 事件（对应 on/off）
    /// </summary>
    public static event System.Action<ChannelStateInfo> ChannelStateChange
    {
        add
        {
            _eventListeners.Add(("channelStateChange", value),
                args => value(new ChannelStateInfo(args[0])),
                js => NodeApi.CallMethodVoid(Module, _on, "channelStateChange", js));
        }
        remove
        {
            _eventListeners.Remove(("channelStateChange", value), js => NodeApi.CallMethodVoid(Module, _off, "channelStateChange", js));
        }
    }

}

/// <summary>
/// ChannelInfo（@ohos 命名空间内嵌套纯数据接口，入参对象）。
/// </summary>
public sealed record ChannelInfo(
    global::HarmonyOS.ArkUI.LinkType LinkType,
    string PeerDevAddr,
    string PeerUuid
) : INapiRecord
{
    void INapiRecord.WriteTo(NativeNodeApi.napi_env env, NativeNodeApi.napi_value obj)
    {
        var _linkType = System.Text.Encoding.UTF8.GetBytes("linkType");
        var _linkTypeV = NativeValue.From(LinkType);
        if (_linkTypeV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _linkType, _linkTypeV);
        var _peerDevAddr = System.Text.Encoding.UTF8.GetBytes("peerDevAddr");
        var _peerDevAddrV = NativeValue.From(PeerDevAddr);
        if (_peerDevAddrV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _peerDevAddr, _peerDevAddrV);
        var _peerUuid = System.Text.Encoding.UTF8.GetBytes("peerUuid");
        var _peerUuidV = NativeValue.From(PeerUuid);
        if (_peerUuidV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _peerUuid, _peerUuidV);
    }
}

/// <summary>
/// DataInfo 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class DataInfo : JsObject
{
    public DataInfo(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _channelId => "channelId"u8;
    private static ReadOnlySpan<byte> _data => "data"u8;
    /// <summary>
    /// channelId
    /// </summary>
    public double ChannelId => NativeValue.ToDouble(GetPropertyRaw(_channelId));

    /// <summary>
    /// data
    /// </summary>
    public byte[] Data => ValueConverter.ConvertArray(GetPropertyRaw(_data), static e => ValueConverter.Convert<byte>(e));

}

/// <summary>
/// ChannelStateInfo 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class ChannelStateInfo : JsObject
{
    public ChannelStateInfo(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _channelId => "channelId"u8;
    private static ReadOnlySpan<byte> _state => "state"u8;
    /// <summary>
    /// channelId
    /// </summary>
    public double ChannelId => NativeValue.ToDouble(GetPropertyRaw(_channelId));

    /// <summary>
    /// state
    /// </summary>
    public global::HarmonyOS.ArkUI.ChannelState State => (global::HarmonyOS.ArkUI.ChannelState)NativeValue.ToInt(GetPropertyRaw(_state));

}
