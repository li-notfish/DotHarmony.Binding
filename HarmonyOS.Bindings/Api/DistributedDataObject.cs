// <auto-generated>
// 由 apiGenerator.ts 生成（@ohos.* namespace 绑定路线）。
// 生成器输入：HarmonyOS SDK .d.ts → astParser → ComponentInfo → 本生成器
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;
using HarmonyOS.Bindings.Runtime;
using HarmonyOS.ArkUI;

namespace HarmonyOS.Bindings.Api;

/// <summary>
/// DistributedDataObject 绑定（@ohos.data.distributedDataObject）。
/// 所需权限：ohos.permission.DISTRIBUTED_DATASYNC
/// </summary>
public static unsafe partial class DistributedDataObject
{
    private const string ModuleName = "@ohos.data.distributedDataObject";

    private static NapiReference? _moduleRef;
    private static bool _loadAttempted;

    /// <summary>懒加载的 @ohos 模块对象（internal：同文件包装类的构造函数需要）</summary>
    internal static IntPtr Module
    {
        get
        {
            if (_moduleRef != null) return _moduleRef.Value;
            if (_loadAttempted)
                throw new InvalidOperationException($"{ModuleName} module failed to load (previous attempt)");
            _loadAttempted = true;

            var env = NapiEnv.Current;
            NativeNodeApi.napi_open_handle_scope(env, out var scope).ThrowIfFailed();
            try
            {
                foreach (var name in new[] { "=" + ModuleName, ModuleName })
                {
                    var utf8 = System.Text.Encoding.UTF8.GetBytes(name);
                    fixed (byte* p = utf8)
                    {
                        var status = NativeNodeApi.napi_load_module(env, p, out var module);
                        if (status == NativeNodeApi.napi_status.napi_ok && module != IntPtr.Zero)
                        {
                            _moduleRef = new NapiReference(module);
                            break;
                        }
                        NativeNodeApi.napi_get_and_clear_last_exception(env, out _);
                    }
                }
            }
            finally
            {
                NativeNodeApi.napi_close_handle_scope(env, scope).ThrowIfFailed();
            }

            if (_moduleRef == null)
                throw new InvalidOperationException(
                    $"failed to load {ModuleName} via napi_load_module (tried with and without '=' prefix)");

            HiLog.Info("HarmonyHost", $"[data.distributedDataObject] module loaded via napi_load_module");
            return _moduleRef.Value;
        }
    }

    private static ReadOnlySpan<byte> _createDistributedObject => "createDistributedObject"u8;
    private static ReadOnlySpan<byte> _create => "create"u8;
    private static ReadOnlySpan<byte> _genSessionId => "genSessionId"u8;

    /// <summary>
    /// createDistributedObject
    /// </summary>
    public static DistributedObject CreateDistributedObject(IntPtr source)
    {
        return NodeApi.CallMethod(Module, _createDistributedObject, static h => new DistributedObject(h), source);
    }

    /// <summary>
    /// create
    /// </summary>
    public static DataObject Create(IntPtr context, IntPtr source)
    {
        return NodeApi.CallMethod(Module, _create, static h => new DataObject(h), context, source);
    }

    /// <summary>
    /// genSessionId
    /// </summary>
    public static string GenSessionId()
    {
        return NodeApi.CallMethod<string>(Module, _genSessionId);
    }

}

/// <summary>
/// DistributedObject 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class DistributedObject : JsObject
{
    public DistributedObject(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _setSessionId => "setSessionId"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;
    /// <summary>
    /// setSessionId
    /// </summary>
    public bool SetSessionId(string? sessionId = null)
    {
        return CallMethod<bool>(_setSessionId, sessionId);
    }

    /// <summary>
    /// on
    /// </summary>
    public void On(string type, IntPtr callback)
    {
        CallMethodVoid(_on, type, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public void Off(string type, IntPtr callback)
    {
        CallMethodVoid(_off, type, callback);
    }

    private readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public void Off(string type)
    {
        NodeApi.CallMethodVoid(Handle, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// 监听 change 事件（对应 on/off）
    /// </summary>
    public event System.Action Change
    {
        add
        {
            _eventListeners.Add(("change", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "change", js));
        }
        remove
        {
            _eventListeners.Remove(("change", value), js => NodeApi.CallMethodVoid(Handle, _off, "change", js));
        }
    }

    /// <summary>
    /// 监听 status 事件（对应 on/off）
    /// </summary>
    public event System.Action Status
    {
        add
        {
            _eventListeners.Add(("status", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "status", js));
        }
        remove
        {
            _eventListeners.Remove(("status", value), js => NodeApi.CallMethodVoid(Handle, _off, "status", js));
        }
    }

}

/// <summary>
/// DataObject 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class DataObject : JsObject
{
    public DataObject(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _setSessionId => "setSessionId"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;
    private static ReadOnlySpan<byte> _save => "save"u8;
    private static ReadOnlySpan<byte> _revokeSave => "revokeSave"u8;
    private static ReadOnlySpan<byte> _bindAssetStore => "bindAssetStore"u8;
    private static ReadOnlySpan<byte> _setAsset => "setAsset"u8;
    private static ReadOnlySpan<byte> _setAssets => "setAssets"u8;
    /// <summary>
    /// setSessionId
    /// </summary>
    public Task SetSessionIdAsync(string sessionId)
    {
        return CallMethodAsyncVoid(_setSessionId, sessionId);
    }

    /// <summary>
    /// setSessionId
    /// </summary>
    public Task SetSessionIdAsync()
    {
        return CallMethodAsyncCallbackVoid(_setSessionId);
    }

    /// <summary>
    /// on
    /// </summary>
    public void On(string type, IntPtr callback)
    {
        CallMethodVoid(_on, type, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public void Off(string type, IntPtr callback)
    {
        CallMethodVoid(_off, type, callback);
    }

    /// <summary>
    /// save
    /// </summary>
    public Task<SaveSuccessResponse> SaveAsync(string deviceId)
    {
        return CallMethodAsync(_save, static h => new SaveSuccessResponse(h), deviceId);
    }

    /// <summary>
    /// revokeSave
    /// </summary>
    public Task<RevokeSaveSuccessResponse> RevokeSaveAsync()
    {
        return CallMethodAsync(_revokeSave, static h => new RevokeSaveSuccessResponse(h));
    }

    /// <summary>
    /// bindAssetStore
    /// </summary>
    public Task BindAssetStoreAsync(string assetKey, IntPtr bindInfo)
    {
        return CallMethodAsyncVoid(_bindAssetStore, assetKey, bindInfo);
    }

    /// <summary>
    /// setAsset
    /// </summary>
    public Task SetAssetAsync(string assetKey, string uri)
    {
        return CallMethodAsyncVoid(_setAsset, assetKey, uri);
    }

    /// <summary>
    /// setAssets
    /// </summary>
    public Task SetAssetsAsync(string assetsKey, string[] uris)
    {
        return CallMethodAsyncVoid(_setAssets, assetsKey, uris);
    }

    private readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public void Off(string type)
    {
        NodeApi.CallMethodVoid(Handle, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// 监听 change 事件（对应 on/off）
    /// </summary>
    public event System.Action Change
    {
        add
        {
            _eventListeners.Add(("change", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "change", js));
        }
        remove
        {
            _eventListeners.Remove(("change", value), js => NodeApi.CallMethodVoid(Handle, _off, "change", js));
        }
    }

    /// <summary>
    /// 监听 status 事件（对应 on/off）
    /// </summary>
    public event System.Action Status
    {
        add
        {
            _eventListeners.Add(("status", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "status", js));
        }
        remove
        {
            _eventListeners.Remove(("status", value), js => NodeApi.CallMethodVoid(Handle, _off, "status", js));
        }
    }

    /// <summary>
    /// 监听 change 事件（对应 on/off）
    /// </summary>
    public event System.Action ChangeEvent
    {
        add
        {
            _eventListeners.Add(("change", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "change", js));
        }
        remove
        {
            _eventListeners.Remove(("change", value), js => NodeApi.CallMethodVoid(Handle, _off, "change", js));
        }
    }

    /// <summary>
    /// 监听 status 事件（对应 on/off）
    /// </summary>
    public event System.Action StatusEvent
    {
        add
        {
            _eventListeners.Add(("status", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "status", js));
        }
        remove
        {
            _eventListeners.Remove(("status", value), js => NodeApi.CallMethodVoid(Handle, _off, "status", js));
        }
    }

    /// <summary>
    /// 监听 progressChanged 事件（对应 on/off）
    /// </summary>
    public event System.Action ProgressChanged
    {
        add
        {
            _eventListeners.Add(("progressChanged", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "progressChanged", js));
        }
        remove
        {
            _eventListeners.Remove(("progressChanged", value), js => NodeApi.CallMethodVoid(Handle, _off, "progressChanged", js));
        }
    }

}

/// <summary>
/// SaveSuccessResponse 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class SaveSuccessResponse : JsObject
{
    public SaveSuccessResponse(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _sessionId => "sessionId"u8;
    private static ReadOnlySpan<byte> _version => "version"u8;
    private static ReadOnlySpan<byte> _deviceId => "deviceId"u8;
    /// <summary>
    /// sessionId
    /// </summary>
    public string SessionId => NativeValue.ToString(GetPropertyRaw(_sessionId)) ?? string.Empty;

    /// <summary>
    /// version
    /// </summary>
    public double Version => NativeValue.ToDouble(GetPropertyRaw(_version));

    /// <summary>
    /// deviceId
    /// </summary>
    public string DeviceId => NativeValue.ToString(GetPropertyRaw(_deviceId)) ?? string.Empty;

}

/// <summary>
/// RevokeSaveSuccessResponse 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class RevokeSaveSuccessResponse : JsObject
{
    public RevokeSaveSuccessResponse(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _sessionId => "sessionId"u8;
    /// <summary>
    /// sessionId
    /// </summary>
    public string SessionId => NativeValue.ToString(GetPropertyRaw(_sessionId)) ?? string.Empty;

}
