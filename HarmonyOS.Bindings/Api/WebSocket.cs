// <auto-generated>
// 由 apiGenerator.ts 生成（@ohos.* namespace 绑定路线）。
// 生成器输入：HarmonyOS SDK .d.ts → astParser → ComponentInfo → 本生成器
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;
using HarmonyOS.Bindings.Runtime;
using HarmonyOS.ArkUI;

namespace HarmonyOS.Bindings.Api;

/// <summary>
/// WebSocket 绑定（@ohos.net.webSocket）。
/// 所需权限：ohos.permission.INTERNET
/// </summary>
public static unsafe partial class WebSocket
{
    private const string ModuleName = "@ohos.net.webSocket";

    private static NapiReference? _moduleRef;
    private static bool _loadAttempted;

    /// <summary>懒加载的 @ohos 模块对象（internal：同文件包装类的构造函数需要）</summary>
    internal static IntPtr Module
    {
        get
        {
            if (_moduleRef != null) return _moduleRef.Value;
            if (_loadAttempted)
                throw new InvalidOperationException($"{ModuleName} module failed to load (previous attempt)");
            _loadAttempted = true;

            var env = NapiEnv.Current;
            NativeNodeApi.napi_open_handle_scope(env, out var scope).ThrowIfFailed();
            try
            {
                foreach (var name in new[] { "=" + ModuleName, ModuleName })
                {
                    var utf8 = System.Text.Encoding.UTF8.GetBytes(name);
                    fixed (byte* p = utf8)
                    {
                        var status = NativeNodeApi.napi_load_module(env, p, out var module);
                        if (status == NativeNodeApi.napi_status.napi_ok && module != IntPtr.Zero)
                        {
                            _moduleRef = new NapiReference(module);
                            break;
                        }
                        NativeNodeApi.napi_get_and_clear_last_exception(env, out _);
                    }
                }
            }
            finally
            {
                NativeNodeApi.napi_close_handle_scope(env, scope).ThrowIfFailed();
            }

            if (_moduleRef == null)
                throw new InvalidOperationException(
                    $"failed to load {ModuleName} via napi_load_module (tried with and without '=' prefix)");

            HiLog.Info("HarmonyHost", $"[net.webSocket] module loaded via napi_load_module");
            return _moduleRef.Value;
        }
    }

    private static ReadOnlySpan<byte> _createWebSocket => "createWebSocket"u8;
    private static ReadOnlySpan<byte> _createWebSocketServer => "createWebSocketServer"u8;

    /// <summary>
    /// createWebSocket
    /// </summary>
    public static WebSocketObject CreateWebSocket()
    {
        return NodeApi.CallMethod(Module, _createWebSocket, static h => new WebSocketObject(h));
    }

    /// <summary>
    /// createWebSocketServer
    /// </summary>
    public static WebSocketServer CreateWebSocketServer()
    {
        return NodeApi.CallMethod(Module, _createWebSocketServer, static h => new WebSocketServer(h));
    }

}

/// <summary>
/// WebSocket 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class WebSocketObject : JsObject
{
    public WebSocketObject(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _connect => "connect"u8;
    private static ReadOnlySpan<byte> _send => "send"u8;
    private static ReadOnlySpan<byte> _close => "close"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;
    /// <summary>
    /// connect
    /// </summary>
    public Task<bool> ConnectAsync(string url)
    {
        return CallMethodAsyncCallback<bool>(_connect, null, url);
    }

    /// <summary>
    /// connect
    /// </summary>
    public Task<bool> ConnectAsync(string url, IntPtr options)
    {
        return CallMethodAsync<bool>(_connect, url, options);
    }

    /// <summary>
    /// send
    /// </summary>
    public Task<bool> SendAsync(string data)
    {
        return CallMethodAsync<bool>(_send, data);
    }

    /// <summary>
    /// close
    /// </summary>
    public Task<bool> CloseAsync()
    {
        return CallMethodAsyncCallback<bool>(_close, null);
    }

    /// <summary>
    /// close
    /// </summary>
    public Task<bool> CloseAsync(WebSocketCloseOptions options)
    {
        return CallMethodAsync<bool>(_close, options);
    }

    /// <summary>
    /// on
    /// </summary>
    public void On(string type, IntPtr callback)
    {
        CallMethodVoid(_on, type, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public void Off(string type, IntPtr callback)
    {
        CallMethodVoid(_off, type, callback);
    }

    private readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<IntPtr> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(args[0]),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public void Off(string type)
    {
        NodeApi.CallMethodVoid(Handle, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<IntPtr> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<string> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback((NativeValue.ToString(args[0]) ?? string.Empty)),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<WebSocketOpenInfo> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(new WebSocketOpenInfo(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<WebSocketOpenInfo> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<string> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<CloseResult> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(new CloseResult(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<CloseResult> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// 监听 open 事件（对应 on/off）
    /// </summary>
    public event System.Action<IntPtr> Open
    {
        add
        {
            _eventListeners.Add(("open", value),
                args => value(args[0]),
                js => NodeApi.CallMethodVoid(Handle, _on, "open", js));
        }
        remove
        {
            _eventListeners.Remove(("open", value), js => NodeApi.CallMethodVoid(Handle, _off, "open", js));
        }
    }

    /// <summary>
    /// 监听 message 事件（对应 on/off）
    /// </summary>
    public event System.Action<string> Message
    {
        add
        {
            _eventListeners.Add(("message", value),
                args => value((NativeValue.ToString(args[0]) ?? string.Empty)),
                js => NodeApi.CallMethodVoid(Handle, _on, "message", js));
        }
        remove
        {
            _eventListeners.Remove(("message", value), js => NodeApi.CallMethodVoid(Handle, _off, "message", js));
        }
    }

    /// <summary>
    /// 监听 openInfo 事件（对应 on/off）
    /// </summary>
    public event System.Action<WebSocketOpenInfo> OpenInfo
    {
        add
        {
            _eventListeners.Add(("openInfo", value),
                args => value(new WebSocketOpenInfo(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "openInfo", js));
        }
        remove
        {
            _eventListeners.Remove(("openInfo", value), js => NodeApi.CallMethodVoid(Handle, _off, "openInfo", js));
        }
    }

    /// <summary>
    /// 监听 close 事件（对应 on/off）
    /// </summary>
    public event System.Action<CloseResult> CloseEvent
    {
        add
        {
            _eventListeners.Add(("close", value),
                args => value(new CloseResult(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "close", js));
        }
        remove
        {
            _eventListeners.Remove(("close", value), js => NodeApi.CallMethodVoid(Handle, _off, "close", js));
        }
    }

    /// <summary>
    /// 监听 error 事件（对应 on/off）
    /// </summary>
    public event System.Action Error
    {
        add
        {
            _eventListeners.Add(("error", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "error", js));
        }
        remove
        {
            _eventListeners.Remove(("error", value), js => NodeApi.CallMethodVoid(Handle, _off, "error", js));
        }
    }

    /// <summary>
    /// 监听 dataEnd 事件（对应 on/off）
    /// </summary>
    public event System.Action DataEnd
    {
        add
        {
            _eventListeners.Add(("dataEnd", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "dataEnd", js));
        }
        remove
        {
            _eventListeners.Remove(("dataEnd", value), js => NodeApi.CallMethodVoid(Handle, _off, "dataEnd", js));
        }
    }

    /// <summary>
    /// 监听 headerReceive 事件（对应 on/off）
    /// </summary>
    public event System.Action<IntPtr> HeaderReceive
    {
        add
        {
            _eventListeners.Add(("headerReceive", value),
                args => value(args[0]),
                js => NodeApi.CallMethodVoid(Handle, _on, "headerReceive", js));
        }
        remove
        {
            _eventListeners.Remove(("headerReceive", value), js => NodeApi.CallMethodVoid(Handle, _off, "headerReceive", js));
        }
    }

}

/// <summary>
/// WebSocketServer 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class WebSocketServer : JsObject
{
    public WebSocketServer(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _start => "start"u8;
    private static ReadOnlySpan<byte> _stop => "stop"u8;
    private static ReadOnlySpan<byte> _listAllConnections => "listAllConnections"u8;
    private static ReadOnlySpan<byte> _send => "send"u8;
    private static ReadOnlySpan<byte> _close => "close"u8;
    private static ReadOnlySpan<byte> _on => "on"u8;
    private static ReadOnlySpan<byte> _off => "off"u8;
    /// <summary>
    /// start
    /// </summary>
    public Task<bool> StartAsync(WebSocketServerConfig config)
    {
        return CallMethodAsync<bool>(_start, config);
    }

    /// <summary>
    /// stop
    /// </summary>
    public Task<bool> StopAsync()
    {
        return CallMethodAsync<bool>(_stop);
    }

    /// <summary>
    /// listAllConnections
    /// </summary>
    public WebSocketConnection[] ListAllConnections()
    {
        return CallMethod(_listAllConnections, h => ValueConverter.ConvertArray(h, static e => new WebSocketConnection(e)));
    }

    /// <summary>
    /// send
    /// </summary>
    public Task<bool> SendAsync(string data, WebSocketConnection connection)
    {
        return CallMethodAsync<bool>(_send, data, connection);
    }

    /// <summary>
    /// close
    /// </summary>
    public Task<bool> CloseAsync(WebSocketConnection connection, IntPtr? options = null)
    {
        return CallMethodAsync<bool>(_close, connection, options);
    }

    /// <summary>
    /// on
    /// </summary>
    public void On(string type, IntPtr callback)
    {
        CallMethodVoid(_on, type, callback);
    }

    /// <summary>
    /// off
    /// </summary>
    public void Off(string type, IntPtr callback)
    {
        CallMethodVoid(_off, type, callback);
    }

    private readonly EventListenerRegistry _eventListeners = new();

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<WebSocketConnection> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(new WebSocketConnection(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type)：移除该事件类型的全部回调
    /// </summary>
    public void Off(string type)
    {
        NodeApi.CallMethodVoid(Handle, _off, type);
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<WebSocketConnection> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action<WebSocketMessage> callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(new WebSocketMessage(args[0])),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action<WebSocketMessage> callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// on(type, callback) 的类型化重载（回调经共享跳板进入 C#，任意参数类型自动转换）
    /// </summary>
    public void On(string type, System.Action callback)
    {
        _eventListeners.Add((type, callback),
            args => callback(),
            js => NodeApi.CallMethodVoid(Handle, _on, type, js));
    }

    /// <summary>
    /// off(type, callback)：解除订阅（按 handler 匹配）
    /// </summary>
    public void Off(string type, System.Action callback)
    {
        _eventListeners.Remove((type, callback), js => NodeApi.CallMethodVoid(Handle, _off, type, js));
    }

    /// <summary>
    /// 监听 connect 事件（对应 on/off）
    /// </summary>
    public event System.Action<WebSocketConnection> Connect
    {
        add
        {
            _eventListeners.Add(("connect", value),
                args => value(new WebSocketConnection(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "connect", js));
        }
        remove
        {
            _eventListeners.Remove(("connect", value), js => NodeApi.CallMethodVoid(Handle, _off, "connect", js));
        }
    }

    /// <summary>
    /// 监听 messageReceive 事件（对应 on/off）
    /// </summary>
    public event System.Action<WebSocketMessage> MessageReceive
    {
        add
        {
            _eventListeners.Add(("messageReceive", value),
                args => value(new WebSocketMessage(args[0])),
                js => NodeApi.CallMethodVoid(Handle, _on, "messageReceive", js));
        }
        remove
        {
            _eventListeners.Remove(("messageReceive", value), js => NodeApi.CallMethodVoid(Handle, _off, "messageReceive", js));
        }
    }

    /// <summary>
    /// 监听 close 事件（对应 on/off）
    /// </summary>
    public event System.Action CloseEvent
    {
        add
        {
            _eventListeners.Add(("close", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "close", js));
        }
        remove
        {
            _eventListeners.Remove(("close", value), js => NodeApi.CallMethodVoid(Handle, _off, "close", js));
        }
    }

    /// <summary>
    /// 监听 error 事件（对应 on/off）
    /// </summary>
    public event System.Action Error
    {
        add
        {
            _eventListeners.Add(("error", value),
                args => value(),
                js => NodeApi.CallMethodVoid(Handle, _on, "error", js));
        }
        remove
        {
            _eventListeners.Remove(("error", value), js => NodeApi.CallMethodVoid(Handle, _off, "error", js));
        }
    }

}

/// <summary>
/// WebSocketOpenInfo 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class WebSocketOpenInfo : JsObject
{
    public WebSocketOpenInfo(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _status => "status"u8;
    private static ReadOnlySpan<byte> _message => "message"u8;
    private static ReadOnlySpan<byte> _protocol => "protocol"u8;
    /// <summary>
    /// status
    /// </summary>
    public double Status => NativeValue.ToDouble(GetPropertyRaw(_status));

    /// <summary>
    /// message
    /// </summary>
    public string Message => NativeValue.ToString(GetPropertyRaw(_message)) ?? string.Empty;

    /// <summary>
    /// protocol
    /// </summary>
    public string? Protocol => (string?)NativeValue.ToString(GetPropertyRaw(_protocol)) ?? string.Empty;

}

/// <summary>
/// CloseResult 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class CloseResult : JsObject
{
    public CloseResult(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _code => "code"u8;
    private static ReadOnlySpan<byte> _reason => "reason"u8;
    /// <summary>
    /// code
    /// </summary>
    public double Code => NativeValue.ToDouble(GetPropertyRaw(_code));

    /// <summary>
    /// reason
    /// </summary>
    public string Reason => NativeValue.ToString(GetPropertyRaw(_reason)) ?? string.Empty;

}

/// <summary>
/// WebSocketConnection 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class WebSocketConnection : JsObject
{
    public WebSocketConnection(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _clientIP => "clientIP"u8;
    private static ReadOnlySpan<byte> _clientPort => "clientPort"u8;
    /// <summary>
    /// clientIP
    /// </summary>
    public string ClientIP => NativeValue.ToString(GetPropertyRaw(_clientIP)) ?? string.Empty;

    /// <summary>
    /// clientPort
    /// </summary>
    public double ClientPort => NativeValue.ToDouble(GetPropertyRaw(_clientPort));

}

/// <summary>
/// WebSocketMessage 实例包装（@ohos 命名空间内嵌套接口）。
/// 由 JsObject 持有 napi 强引用；Dispose 仅释放引用，JS 对象由 ArkTS GC 管理。
/// </summary>
public sealed partial class WebSocketMessage : JsObject
{
    public WebSocketMessage(IntPtr handle) : base(handle) { }
    private static ReadOnlySpan<byte> _clientConnection => "clientConnection"u8;
    private static ReadOnlySpan<byte> _data => "data"u8;
    /// <summary>
    /// clientConnection
    /// </summary>
    public WebSocketConnection ClientConnection => new WebSocketConnection(GetPropertyRaw(_clientConnection));

    /// <summary>
    /// data
    /// </summary>
    public string Data => NativeValue.ToString(GetPropertyRaw(_data)) ?? string.Empty;

}

/// <summary>
/// WebSocketCloseOptions（@ohos 命名空间内嵌套纯数据接口，入参对象）。
/// </summary>
public sealed record WebSocketCloseOptions(
    double? Code = null,
    string? Reason = null
) : INapiRecord
{
    private static ReadOnlySpan<byte> _codeName => "code"u8;
    private static ReadOnlySpan<byte> _reasonName => "reason"u8;
    void INapiRecord.WriteTo(NativeNodeApi.napi_env env, NativeNodeApi.napi_value obj)
    {
        var _codeV = NativeValue.From(Code);
        if (_codeV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _codeName, _codeV);
        var _reasonV = NativeValue.From(Reason);
        if (_reasonV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _reasonName, _reasonV);
    }
}

/// <summary>
/// WebSocketServerConfig（@ohos 命名空间内嵌套纯数据接口，入参对象）。
/// </summary>
public sealed record WebSocketServerConfig(
    double MaxConnectionsForOneClient,
    double MaxConcurrentClientsNumber,
    double ServerPort,
    string? ServerIP = null,
    ServerCert? ServerCert = null,
    string? Protocol = null
) : INapiRecord
{
    private static ReadOnlySpan<byte> _maxConnectionsForOneClientName => "maxConnectionsForOneClient"u8;
    private static ReadOnlySpan<byte> _maxConcurrentClientsNumberName => "maxConcurrentClientsNumber"u8;
    private static ReadOnlySpan<byte> _serverPortName => "serverPort"u8;
    private static ReadOnlySpan<byte> _serverIPName => "serverIP"u8;
    private static ReadOnlySpan<byte> _serverCertName => "serverCert"u8;
    private static ReadOnlySpan<byte> _protocolName => "protocol"u8;
    void INapiRecord.WriteTo(NativeNodeApi.napi_env env, NativeNodeApi.napi_value obj)
    {
        var _maxConnectionsForOneClientV = NativeValue.From(MaxConnectionsForOneClient);
        if (_maxConnectionsForOneClientV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _maxConnectionsForOneClientName, _maxConnectionsForOneClientV);
        var _maxConcurrentClientsNumberV = NativeValue.From(MaxConcurrentClientsNumber);
        if (_maxConcurrentClientsNumberV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _maxConcurrentClientsNumberName, _maxConcurrentClientsNumberV);
        var _serverPortV = NativeValue.From(ServerPort);
        if (_serverPortV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _serverPortName, _serverPortV);
        var _serverIPV = NativeValue.From(ServerIP);
        if (_serverIPV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _serverIPName, _serverIPV);
        var _serverCertV = NativeValue.From(ServerCert);
        if (_serverCertV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _serverCertName, _serverCertV);
        var _protocolV = NativeValue.From(Protocol);
        if (_protocolV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _protocolName, _protocolV);
    }
}

/// <summary>
/// ServerCert（@ohos 命名空间内嵌套纯数据接口，入参对象）。
/// </summary>
public sealed record ServerCert(
    string CertPath,
    string KeyPath
) : INapiRecord
{
    private static ReadOnlySpan<byte> _certPathName => "certPath"u8;
    private static ReadOnlySpan<byte> _keyPathName => "keyPath"u8;
    void INapiRecord.WriteTo(NativeNodeApi.napi_env env, NativeNodeApi.napi_value obj)
    {
        var _certPathV = NativeValue.From(CertPath);
        if (_certPathV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _certPathName, _certPathV);
        var _keyPathV = NativeValue.From(KeyPath);
        if (_keyPathV != IntPtr.Zero)
            NativeNodeApi.napi_set_named_property(env, obj, _keyPathName, _keyPathV);
    }
}
